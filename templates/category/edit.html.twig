{% extends 'base.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}
    <h1>Edit Category</h1>

    {{ form_start(test_form) }}
    {{ form_widget(test_form) }}
    <button class="btn">{{ button_label|default('sa') }}</button>
    {{ form_end(test_form) }}


    <a href="{{ path('category_index') }}">back to list</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
